// other state or sources

// inner state
const state = {
  value: ''
}

const mutations = {
  SET_VALUE: (state, value) => {
    state.value = value
  }
}

const actions = {
  // async
  async asyncHandle({ commit, state, dispatch }) {
    // const res = await apiHandle()
    // if (res.code === 0) {
    //   // update state
    // }
  },
  // temp
  tempSetValue({ commit, state, dispatch }, val) {
    commit('SET_VALUE', val)
  }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}

