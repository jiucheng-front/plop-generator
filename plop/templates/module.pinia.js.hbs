import { defineStore } from 'pinia'

// Template
const use{{firstUpperCase moduleName}}Store = defineStore('{{moduleName}}', {
    state: () => {
        return {
            tempInfo: {},
        }
    },
    getters: {
        getTempInfo: (state) => {
            return state.tempInfo
        },
    },
    actions: {
        setTempInfo(obj) {
            this.tempInfo = obj
        },
    },
})

export default use{{firstUpperCase moduleName}}Store
